#include <iostream>
#include <vector>
using namespace std;

int main(){
    int n, input, target;
    cin >> n;
    vector<int> price;
    vector<pair<int, int>> save;
    while(n--){
        cin >> input;
        price.push_back(input);
    }
    cin >> target;

    
}